キャッシュの移行期間の問題解決をサポートするモジュールです。

** 概要

- 更新リクエストは新旧両方のメソッドが呼ばれます。
- 取得リクエストは古いキャッシュオブジェクトが使われます。
- 時間経過後は、新しいキャッシュオブジェクトが使われます。


** 注意と制限

私はCache::Memcached::Fastユーザーなので、Cache::Memcacehd::Fastを想定して書かれています。
get/setを使う分には他のCache::* モジュールとも互換があるでしょう。
versionやflush_allなど他のメソッドはdelegateしません。新旧どちらに投げるべきなのか判断できないので。

